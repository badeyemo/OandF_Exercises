/*
a.  Returns ‘Tim’ because the variable was defined inside the function displayFirstName that is being invoked.
b.  Returns a reference error firstName not defined because console.log can’t access the firstName as it is a local scope that can only be accessed by invoking its function.
c.	The code returns ‘Elie’ because the variable firstName is declared as a global scope and can be accessed from anywhere.
d.	The code returns reference error because firstName is not declared as a variable.
e.	Returns “Hi undefined”.
f.	Returns “Hi Elie”.
g.	Returns “Hi”.
h.	Returns “Hi”.  */

